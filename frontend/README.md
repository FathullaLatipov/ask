# Employee Management Dashboard

Современный веб-дашборд для системы учета сотрудников.

## Технологии

- **React 19** - UI библиотека
- **Vite** - сборщик и dev-сервер
- **React Router** - маршрутизация
- **Recharts** - графики и диаграммы
- **Axios** - HTTP клиент

## Структура

```
frontend/
├── src/
│   ├── api/           # API клиент
│   ├── components/     # Переиспользуемые компоненты
│   │   ├── Layout.jsx  # Основной layout с навигацией
│   │   └── ProtectedRoute.jsx  # Защита маршрутов
│   ├── pages/         # Страницы приложения
│   │   ├── Login.jsx   # Страница входа
│   │   ├── Dashboard.jsx  # Главная страница со статистикой
│   │   ├── Attendance.jsx  # Учет рабочего времени
│   │   ├── Users.jsx   # Управление сотрудниками
│   │   ├── Salary.jsx  # Расчет зарплаты
│   │   ├── Requests.jsx  # Заявки
│   │   └── Departments.jsx  # Отделы
│   ├── App.jsx        # Главный компонент с роутингом
│   └── main.jsx       # Точка входа
```

## Установка и запуск

1. Установите зависимости (если еще не установлены):
```bash
npm install
```

2. Создайте файл `.env.local` в корне `frontend/`:
```
VITE_API_BASE_URL=http://127.0.0.1:8000
```

3. Запустите dev-сервер:
```bash
npm run dev
```

4. Откройте браузер по адресу, который покажет Vite (обычно `http://localhost:5173`)

## Функционал

### 📊 Dashboard (Главная)
- Статистика: всего сотрудников, активных, отделов, заявок
- График посещаемости за неделю
- Круговая диаграмма распределения по отделам
- Быстрые действия для перехода к модулям

### ⏰ Посещаемость
- Текущий статус сотрудника
- Отметка прихода/ухода
- Список активных сотрудников
- График отработанных часов
- История посещений

### 👥 Сотрудники
- Список всех сотрудников
- Фильтрация по отделам и ролям
- Информация о должности и статусе

### 💰 Зарплата
- Расчет зарплаты за период
- История расчетов
- Детализация по часам и штрафам

### 📝 Заявки
- Список всех заявок (отпуск, больничный, аванс)
- Фильтрация по статусу
- Утверждение/отклонение заявок

### 🏢 Отделы
- Список отделов
- Распределение сотрудников (круговая диаграмма)
- Детальная информация по каждому отделу

## API Endpoints

Дашборд использует следующие эндпоинты бэкенда:

- `POST /api/auth/login` - Вход
- `GET /api/users/me` - Текущий пользователь
- `GET /api/users/` - Список пользователей
- `GET /api/attendance/current/` - Текущий статус
- `GET /api/attendance/history/` - История посещений
- `GET /api/attendance/active/` - Активные сотрудники
- `POST /api/attendance/checkin/` - Отметка прихода
- `POST /api/attendance/checkout/` - Отметка ухода
- `GET /api/departments/` - Список отделов
- `GET /api/salary/` - Расчеты зарплаты
- `POST /api/salary/calculate/` - Расчет зарплаты
- `GET /api/requests/` - Список заявок
- `POST /api/requests/:id/approve/` - Утвердить заявку
- `POST /api/requests/:id/reject/` - Отклонить заявку

## Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`.

## Документация API

После запуска бэкенда доступна документация:
- Swagger UI: http://127.0.0.1:8000/swagger/
- ReDoc: http://127.0.0.1:8000/redoc/
